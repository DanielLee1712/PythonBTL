# VNPAY keys for Docker are read from the PROJECT ROOT ".env"
# (next to docker-compose.yml). Copy root ".env.example" → ".env" and fill values.
#
# For payment-service without Docker (local manage.py), export the same vars or use:
#   set -a; source ../.env; set +a
